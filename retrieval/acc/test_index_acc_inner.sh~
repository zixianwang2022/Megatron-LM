#!/bin/bash

# DIR=$(readlink -f `pwd`)
# echo $DIR
# exit

if [ "$#" -ne 3 ]; then
    echo "illegal num args, $#."
    exit 1
fi

task=$1
index_path_0=$2
index_path_1=$3

pip install h5py
python -m lawrence.test_index_acc --task $task --i0 $index_path_0 --i1 $index_path_1

# eof
